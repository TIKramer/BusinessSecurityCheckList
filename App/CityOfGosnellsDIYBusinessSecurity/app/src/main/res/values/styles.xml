<resources>

    <!-- Base application theme.-->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="AppThemeLargeText">
        <!-- Customize your theme here. -->
        <item name="android:textSize">22sp</item>
    </style>

    <!-- Action bar theme -->
    <style name="MyActionBar" parent="@style/Widget.AppCompat.Light.ActionBar">
        <item name="android:background">@drawable/gosnells_bar</item>
        <item name="background">@drawable/gosnells_bar</item>
    </style>

    <style name="checklistTheme" parent="@android:style/Theme.Holo">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:tabStripEnabled">true</item>
        <item name="android:actionBarTabStyle">@style/MyActionBarTabs</item>
        <item name="android:actionBarStyle">@style/MyChecklistActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/MyActionBarTabText</item>

    </style>

    <!-- Styles the Tabs in the action bar -->
    <style name="MyActionBarTabs" parent="@android:style/Widget.Holo.ActionBar.TabView">
        <item name="android:background">@color/orange</item>
    </style>

    <!-- Style for the action bar -->
    <style name="MyChecklistActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@drawable/gosnells_bar</item>
        <item name="background">@drawable/gosnells_bar</item>
        <item name="android:displayOptions"></item>
    </style>

    <!-- Style for the text in the tabs
    Text Colour in the action Tabs -->
    <style name="MyActionBarTabText" parent="@android:style/Widget.Holo.ActionBar.TabText">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">18sp</item>
    </style>


</resources>
